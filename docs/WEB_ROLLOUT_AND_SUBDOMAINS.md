### WorkPhotoPro Web Rollout, Subdomains, and Feature Implementation\n\n**TL;DR**\n- **Build order**: Ship web endpoints/pages for Reports and Invites first, then the full web app once shared backend and design system are stable.\n- **Subdomains**: Use `app.workphotopro.com`, `reports.workphotopro.com`, and `invite.workphotopro.com` for clear boundaries, caching, and security. Implement with one codebase using host-based routing. Split later if needed.\n\n---\n\n## Rollout Phases\n\n- **Phase 1 (fast)**\n  - Public report pages (SEO/shareable): `reports.workphotopro.com/r/[reportIdOrSlug]`\n  - Team invite accept flow: `invite.workphotopro.com/i/[token]`\n  - Keep mobile app unchanged; add deep link support for invites\n\n- **Phase 2**\n  - Authenticated web app at `app.workphotopro.com` (jobs, chats, basic admin)\n\n- **Phase 3**\n  - Parity features, advanced filters, performance hardening, access controls, org-level admin\n\n---\n\n## Subdomain Strategy\n\n- **Why subdomains**\n  - Clear ownership and boundaries per surface\n  - Better caching and SEO for public reports\n  - Simpler security policies (CSP) and cookies per host\n  - Flexible scaling and future repo/app splits\n\n- **How**\n  - Start with a single Next.js app that serves all three surfaces via host-based routing\n  - Map subdomains to route trees (rewrite prefixes). Split repos later only if warranted\n\n---\n\n## Suggested Next.js App Structure\n\n- DNS: point `app.`, `reports.`, `invite.` to your host (e.g., Vercel/CloudFront/Nginx)\n- Middleware: route by `Host` header to specific base paths\n\n```ts\n// middleware.ts\nimport { NextResponse } from 'next/server';\nimport type { NextRequest } from 'next/server';\n\nexport function middleware(req: NextRequest) {\n  const host = req.headers.get('host') || '';\n  const url = new URL(req.url);\n\n  // reports subdomain\n  if (host.startsWith('reports.')) {\n    if (!url.pathname.startsWith('/r/')) {\n      url.pathname = `/r${url.pathname}`;\n      return NextResponse.rewrite(url);\n    }\n    return NextResponse.next();\n  }\n\n  // invite subdomain\n  if (host.startsWith('invite.')) {\n    if (!url.pathname.startsWith('/i/')) {\n      url.pathname = `/i${url.pathname}`;\n      return NextResponse.rewrite(url);\n    }\n    return NextResponse.next();\n  }\n\n  // default to app subdomain\n  if (host.startsWith('app.')) {\n    if (!url.pathname.startsWith('/app')) {\n      url.pathname = `/app${url.pathname}`;\n      return NextResponse.rewrite(url);\n    }\n  }\n\n  return NextResponse.next();\n}\n```\n\n- Route structure\n  - `app/app/**` → authenticated app UI\n  - `app/r/[reportIdOrSlug]/page.tsx` → public report\n  - `app/i/[token]/page.tsx` → invite accept\n\n- Rendering & caching\n  - Use per-surface layouts and CSP headers\n  - Public report routes: statically cacheable or ISR (revalidate on updates)\n\n---\n\n## Reports Feature (Publish Chat Images to URL)\n\n- **Data model**\n  - `Report { id, slug, jobId, title, visibility: 'public'|'unlisted'|'private', createdBy, createdAt }`\n  - `ReportMedia { id, reportId, storageKey, caption, orderIndex, createdAt }`\n\n- **Generation**\n  - Create a report from selected chat messages/media; snapshot links to files (do not duplicate blobs)\n  - Generate a unique slug (e.g., 8-char random + human title)\n\n- **Access control**\n  - `public`/`unlisted` viewable without auth; `private` requires auth or signed access\n  - Never expose raw storage keys; serve via CDN or short-lived signed URLs\n\n- **Delivery**\n  - Store media on S3/GCS with a CDN; provide transformed renditions (webp, responsive sizes)\n  - Add OG tags for social previews\n  - Optional: PDF export endpoint later\n\n---\n\n## Invite Feature (Unique Team Link)\n\n- **Data model**\n  - `TeamInvite { token, teamId, role, emailOptional, invitedBy, expiresAt, usedAt, state }`\n\n- **Flow**\n  - Create token (crypto-random, 32–48 bytes base64/hex), 7–14 day expiry, single-use\n  - Link: `https://invite.workphotopro.com/i/<token>`\n  - Landing page:\n    - If not authenticated → sign-in/sign-up → return to invite page\n    - Validate token (exists, not expired/used, correct tenant)\n    - Show team/role; on accept → create/attach membership; mark invite used; redirect to `https://app.workphotopro.com/app/teams/<teamId>`\n\n- **Security**\n  - Rate-limit validation endpoint; allow revoke\n  - Audit logs (invited by, accepted by, timestamps)\n\n---\n\n## Auth, Multi-tenancy, and Web App\n\n- **Auth**: reuse mobile provider (OIDC/Supabase/Auth0/Cognito). Share JWT/session validation on server routes\n- **Tenant model**: include `teamId` in session; enforce row-level checks server-side; do not trust client-sent `teamId`\n- **UI**: share design tokens; build a web UI kit if RN components are not shareable\n- **Uploads**: direct-to-storage signed POST; web thumbnails and responsive assets\n\n---\n\n## Minimal Backend/API Endpoints\n\n- Reports\n  - `POST /api/reports` → create report from job/chat selection\n  - `GET /api/reports/:slug` → public-safe report meta\n  - `GET /api/reports/:slug/media` → paginated media with signed URLs\n\n- Invites\n  - `POST /api/invites` → create invite (auth required)\n  - `GET /api/invites/:token` → validate invite (public)\n  - `POST /api/invites/:token/accept` → accept invite (auth required)\n\n- Storage\n  - S3-style with private ACL; short-lived signed GETs for viewers\n  - Optional: generate low-res public derivatives for fully static public reports\n\n---\n\n## Hosting & Infrastructure\n\n- Start simple: one Next.js app on Vercel/Netlify/Fly.io\n- Custom domains\n  - `workphotopro.com` → marketing\n  - `app.workphotopro.com`, `reports.workphotopro.com`, `invite.workphotopro.com` → app surfaces\n- Edge middleware for host routing (above) or per-domain rewrites\n- Caching\n  - Public report pages: ISR with revalidation on updates\n  - Media via CDN with long TTL; cache-bust by file hash\n- Observability: tag logs/metrics by host; error tracking (Sentry)\n\n---\n\n## Security Checklist\n\n- Cryptographically strong tokens and slugs; avoid guessable IDs\n- Signed URLs for media (60–300s) and never expose storage keys\n- CSRF protection for invite acceptance (double-submit or same-site cookies)\n- Rate-limit public endpoints (report views, invite validate)\n- Audit logs for invite creation/acceptance and report publish/unpublish\n- Allow report unpublish/regenerate slug, and invite revoke\n\n---\n\n## Suggested Order of Work\n\n1) Backend models + endpoints for Reports and Invites\n2) Minimal web pages on `reports.` and `invite.` with the flows above\n3) Deploy DNS + domain mapping + middleware host routing\n4) Add CDN image delivery + signed URLs\n5) Build `app.` shell (auth, team switcher), then incrementally add web features\n\n*** End Patch***}ં**assistantҳаҭassistant\Seederка to=functions.apply_patchитель مهالкиочки JsonRequest‍പassistant to=functions.apply_patch.arguments code Execution Returned an Error:  Eve patch error: Failed to parse patch. Error: Could not parse args as JSON: Expecting value: line 1 column 1 (char 0)assistantුවන්analysis|--------------------------------------------------------------------------

